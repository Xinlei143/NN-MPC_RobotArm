# Model-C Workflow

Model C is the MPC-induced data-collection workflow. It is separate from the
frozen Model-A paper controller. Use its scripts to collect closed-loop data,
create a manifest, train a compatible dynamics model, and evaluate it with a
matched reference and RobotSpec. Model-C outputs must not be mixed with Model-A
evidence without an explicit new experiment protocol.
